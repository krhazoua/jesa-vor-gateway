import { useEffect, useMemo, useState } from "react";
import { Download, FileCheck2, LockKeyhole } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { downloadCsvReport, downloadPdfReport, type ReportDefinition } from "@/lib/reportExport";

type FileEvidence = { name: string; data: string };
type ReconciliationSummary = { id: number; recordType: "EQUIPMENT" | "VARIABLE"; authoritySourceRef: string; filename: string; rowCount: number; matchedCount: number; addedCount: number; changedCount: number; removedCount: number; status: "MATCHED" | "MISMATCH" | "BLOCKED"; fatSatGate: "BLOCKED" | "PENDING_EXTERNAL_SIGNOFF"; createdAt: Date };

function readEvidence(file: File): Promise<FileEvidence> { return new Promise((resolve, reject) => { const reader = new FileReader(); reader.onerror = () => reject(new Error("Evidence file could not be read.")); reader.onload = () => resolve({ name: file.name, data: String(reader.result) }); reader.readAsDataURL(file); }); }

export default function ReconciliationEvidencePanel() {
  const [selectedRunId, setSelectedRunId] = useState<number | null>(null);
  const [diffPage, setDiffPage] = useState(1);
  const [certificateSubject, setCertificateSubject] = useState("");
  const [certificateFingerprint, setCertificateFingerprint] = useState("");
  const [referenceId, setReferenceId] = useState("");
  const [certificate, setCertificate] = useState<FileEvidence | null>(null);
  const [reference, setReference] = useState<FileEvidence | null>(null);
  const [comment, setComment] = useState("");
  const runsQuery = trpc.catalog.reconciliationHistory.useQuery(undefined, { retry: false });
  const gateQuery = trpc.catalog.masterGate.useQuery(undefined, { retry: false });
  const activeId = selectedRunId ?? runsQuery.data?.[0]?.id ?? 0;
  const run = (runsQuery.data as ReconciliationSummary[] | undefined)?.find(item => item.id === activeId) ?? null;
  const diffsQuery = trpc.catalog.diffs.useQuery({ id: activeId, page: diffPage, pageSize: 25 }, { enabled: activeId > 0, retry: false });
  const signoffsQuery = trpc.catalog.signoffs.useQuery({ id: activeId }, { enabled: activeId > 0, retry: false });
  const reportQuery = trpc.catalog.report.useQuery({ id: activeId }, { enabled: activeId > 0, retry: false });
  const signoffMutation = trpc.catalog.signoff.useMutation({ onSuccess: value => { toast.success(`External ${value.decision.toLowerCase()} sign-off persisted.`); void signoffsQuery.refetch(); void gateQuery.refetch(); }, onError: error => toast.error(error.message) });
  const armMutation = trpc.catalog.armMaster.useMutation({ onSuccess: value => { toast.success(`Authoritative master gate ${value.status}.`); void gateQuery.refetch(); }, onError: error => toast.error(error.message) });
  useEffect(() => { setDiffPage(1); }, [activeId]);
  const report = useMemo<ReportDefinition | null>(() => { if (!reportQuery.data) return null; return { filename: `jesa-reconciliation-${reportQuery.data.run.id}`, title: "JESA VoR Gateway — Reconciliation report", metadata: { "Reconciliation run": String(reportQuery.data.run.id), "Authority source": reportQuery.data.run.authoritySourceRef, "Record type": reportQuery.data.run.recordType, Status: reportQuery.data.run.status, "FAT/SAT gate": reportQuery.data.run.fatSatGate, Generated: new Date().toISOString(), Boundary: "Evidence only · no plant write path" }, sections: [{ title: "Differences", columns: ["Sequence", "Tag", "Kind", "Details"], rows: reportQuery.data.diffs.map(diff => [diff.sequence, diff.tag, diff.kind, diff.details]) }] }; }, [reportQuery.data]);
  const submitSignoff = () => { if (!activeId || !certificate || !reference) return; signoffMutation.mutate({ reconciliationRunId: activeId, certificateSubject, certificateFingerprint, certificateFile: certificate.data, referenceId, referenceFile: reference.data, decision: "APPROVED", comment: comment || undefined }); };
  return <section className="module-panel reconciliation-evidence-panel"><div className="catalog-import-head"><div><div className="eyebrow cobalt">RECONCILIATION EVIDENCE / CONTROLLED GATE</div><h2>Review, sign, and report authority evidence</h2><p className="module-note">Select a persisted dry-run to inspect all differences, download a report, and capture certificate-backed external sign-off. Master integration remains disabled until these controls are satisfied.</p></div><span className="import-boundary">{gateQuery.data?.status || "DISABLED"}</span></div>{runsQuery.isLoading ? <p className="module-note">Loading reconciliation history…</p> : !runsQuery.data?.length ? <div className="empty-state"><div className="eyebrow">NO RECONCILIATION RUNS</div><h3>Run a dry-run comparison above before capturing sign-off evidence.</h3></div> : <><div className="reconciliation-run-select"><label>RECONCILIATION RUN<select value={activeId} onChange={event => setSelectedRunId(Number(event.target.value))}>{runsQuery.data.map(item => <option key={item.id} value={item.id}>#{item.id} · {item.recordType} · {item.authoritySourceRef} · {item.status}</option>)}</select></label>{run && <div className="reconciliation-run-summary"><strong>{run.status}</strong><span>{run.matchedCount} matched · {run.addedCount} added · {run.changedCount} changed · {run.removedCount} removed</span><small>FAT/SAT: {run.fatSatGate}</small></div>}</div>{diffsQuery.data && <div className="reconciliation-diff-table"><div className="eyebrow">DIFF EVIDENCE / {diffsQuery.data.total} TOTAL</div>{diffsQuery.data.rows.length ? diffsQuery.data.rows.map(row => <div className="reconciliation-diff-row" key={row.id}><span className="mono">{row.sequence}</span><strong>{row.tag}</strong><span>{row.kind}</span><p>{row.details}</p></div>) : <p className="module-note">No differences recorded for this run.</p>}<div className="audit-pagination"><span>Showing {diffsQuery.data.total ? (diffPage - 1) * 25 + 1 : 0}–{Math.min(diffPage * 25, diffsQuery.data.total)} of {diffsQuery.data.total}</span><div className="audit-page-controls"><button type="button" onClick={() => setDiffPage(page => Math.max(1, page - 1))} disabled={diffPage === 1}>Previous</button><span>Page {diffPage} / {Math.max(1, Math.ceil(diffsQuery.data.total / 25))}</span><button type="button" onClick={() => setDiffPage(page => page + 1)} disabled={diffPage >= Math.max(1, Math.ceil(diffsQuery.data.total / 25))}>Next</button></div></div></div>}{report && <div className="reconciliation-report-actions"><button type="button" onClick={() => downloadCsvReport(report)}><Download size={13} /> DOWNLOAD CSV REPORT</button><button type="button" onClick={() => downloadPdfReport(report)}><Download size={13} /> DOWNLOAD PDF REPORT</button></div>}{run?.status === "MATCHED" && <div className="reconciliation-signoff"><div className="eyebrow amber-label">EXTERNAL SIGN-OFF / CERTIFICATE EVIDENCE</div><div className="reconciliation-signoff-grid"><label>CERTIFICATE SUBJECT<input value={certificateSubject} onChange={event => setCertificateSubject(event.target.value)} placeholder="CN=External Reviewer" /></label><label>CERTIFICATE FINGERPRINT<input value={certificateFingerprint} onChange={event => setCertificateFingerprint(event.target.value)} placeholder="SHA-256 fingerprint" /></label><label>REFERENCE ID<input value={referenceId} onChange={event => setReferenceId(event.target.value)} placeholder="FAT-SAT-REF-001" /></label><label className="catalog-file-input">CERTIFICATE FILE<input type="file" accept=".pem,.cer,.crt,.pdf,.txt" onChange={event => { const file = event.target.files?.[0]; if (file) void readEvidence(file).then(setCertificate).catch(error => toast.error(error.message)); }} /><span>{certificate?.name || "Select certificate evidence"}</span></label><label className="catalog-file-input">REFERENCE FILE<input type="file" accept=".pdf,.txt,.csv,.xlsx" onChange={event => { const file = event.target.files?.[0]; if (file) void readEvidence(file).then(setReference).catch(error => toast.error(error.message)); }} /><span>{reference?.name || "Select FAT/SAT reference"}</span></label><label className="reconciliation-signoff-wide">SIGN-OFF COMMENT<textarea value={comment} onChange={event => setComment(event.target.value)} placeholder="Independent review scope and limitations" /></label></div><button type="button" className="primary-btn" onClick={submitSignoff} disabled={signoffMutation.isPending || !certificateSubject.trim() || !certificateFingerprint.trim() || !referenceId.trim() || !certificate || !reference}><FileCheck2 size={14} /> {signoffMutation.isPending ? "PERSISTING SIGN-OFF…" : "CAPTURE APPROVED SIGN-OFF"}</button></div>}{signoffsQuery.data?.length ? <div className="catalog-import-history"><div className="eyebrow">EXTERNAL SIGN-OFF HISTORY</div>{signoffsQuery.data.map(signoff => <div className="catalog-import-history-row" key={signoff.id}><span>{signoff.decision} · {signoff.referenceId}</span><strong>{signoff.certificateFingerprint}</strong><small>{signoff.certificateSubject} · <a href={signoff.certificateStorageUrl} target="_blank" rel="noreferrer">CERTIFICATE</a> · <a href={signoff.referenceStorageUrl} target="_blank" rel="noreferrer">REFERENCE</a></small></div>)}</div> : null}{run?.status === "MATCHED" && signoffsQuery.data?.some(signoff => signoff.decision === "APPROVED") && <div className="master-gate-action"><LockKeyhole size={15} /><span>Certificate-backed external approval is present. Administrator action may arm the master for FAT/SAT only; plant writes remain disabled.</span><button type="button" className="primary-btn" onClick={() => armMutation.mutate({ reconciliationRunId: activeId })} disabled={armMutation.isPending || gateQuery.data?.status === "ARMED_FOR_FAT_SAT"}>{gateQuery.data?.status === "ARMED_FOR_FAT_SAT" ? "MASTER ARMED FOR FAT/SAT" : "ARM MASTER FOR FAT/SAT"}</button></div>}</>}</section>;
}
